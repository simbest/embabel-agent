# 从零开始理解 Embabel Agent：以“合同智审”应用为例

本文面向完全不了解本项目技术栈的开发者，目标是帮助你理解：

1. `embabel-agent` 是什么。
2. 开发“合同智审”应用时，哪些业务场景适合用它。
3. 每个业务场景应该使用哪些 Embabel 组件。
4. 大致应该编写怎样的领域对象、Agent、Action 和 Web API。

本文中的代码都是**简短伪代码**，用于建立概念，不保证可直接复制运行。

---

## 1. 先用一句话理解本项目

`embabel-agent` 是一个基于 JVM、Kotlin/Java、Spring Boot、Spring AI 的 Agent 框架。

它不是一个现成的“合同审查系统”，而是一套帮助你开发智能业务系统的框架。

在合同智审场景中，它可以帮你把下面这些能力组织起来：

```text
合同上传
  ↓
合同文本解析
  ↓
基本信息抽取
  ↓
合同类型识别
  ↓
条款切分与分类
  ↓
规则风险检查
  ↓
AI 语义风险检查
  ↓
企业制度 / 标准条款库检索
  ↓
修改建议生成
  ↓
人工复核
  ↓
审查报告生成
```

传统系统往往是：

```text
Controller → Service → 调用一次大模型 → 返回文本
```

Embabel 更推荐：

```text
Controller
  → AgentPlatform
  → Agent
  → 多个 Action
  → 结构化领域对象
  → Goal 结果
```

也就是说，你不是让大模型一次性“审完整个合同”，而是把合同审查拆成多个明确步骤，每一步都有输入、输出和可测试的业务对象。

---

## 2. 必须先理解的几个概念

### 2.1 Agent

Agent 是一组智能业务能力的集合。

在合同智审里，你可以理解为：

```text
ContractReviewAgent = 专门负责合同审查的智能组件
```

它内部可以包含多个动作，例如抽取信息、识别风险、生成报告。

伪代码：

```java
@Agent(description = "合同智能审查 Agent")
public class ContractReviewAgent {
    // 这里放多个 @Action 方法
}
```

---

### 2.2 Action

Action 是 Agent 可以执行的一个步骤。

合同智审常见 Action：

```text
extractMetadata       抽取合同基本信息
classifyContractType  识别合同类型
splitClauses          切分合同条款
detectRisks           识别合同风险
generateSuggestions   生成修改建议
generateReport        生成审查报告
```

伪代码：

```java
@Action
public ContractMetadata extractMetadata(ContractText contractText, Ai ai) {
    return ai.createObject(
        "从合同正文中抽取合同名称、甲方、乙方、金额、生效日期",
        ContractMetadata.class
    );
}
```

---

### 2.3 Goal

Goal 是 Agent 要达成的目标。

合同智审里的 Goal 可以是：

```text
生成一份合同审查报告
```

伪代码：

```java
@AchievesGoal(description = "生成合同审查报告")
@Action
public ContractReviewReport generateReport(
    ContractMetadata metadata,
    List<ContractRiskFinding> risks,
    Ai ai
) {
    return ai.createObject("根据基本信息和风险清单生成审查报告", ContractReviewReport.class);
}
```

---

### 2.4 Condition

Condition 是判断条件。

例如：

```text
是否已经识别合同类型
是否存在高风险条款
是否需要人工复核
是否需要走数据安全审批
```

伪代码：

```java
@Condition(description = "是否存在高风险问题")
public boolean hasHighRisk(List<ContractRiskFinding> risks) {
    return risks.stream().anyMatch(r -> r.level() == RiskLevel.HIGH);
}
```

---

### 2.5 Domain Model

Domain Model 是业务对象。

合同智审系统不要只返回一段自然语言文本，而应该尽量返回结构化对象。

例如合同基本信息：

```java
public record ContractMetadata(
    String contractTitle,
    String contractType,
    String partyA,
    String partyB,
    String amount,
    String effectiveDate,
    String expiryDate
) {}
```

风险项：

```java
public record ContractRiskFinding(
    String clauseTitle,
    RiskLevel level,
    String description,
    String legalOrPolicyBasis,
    String suggestion
) {}
```

审查报告：

```java
public record ContractReviewReport(
    ContractMetadata metadata,
    List<ContractRiskFinding> risks,
    String summary,
    String conclusion
) {}
```

---


## 3. Embabel 预置 Agent 模式全局矩阵

这一节先暂时跳出“合同智审”，从框架全局理解 Embabel 支持哪些 Agent 模式。合同只是一个例子；这些模式同样可以迁移到投研、风控、运维、政务、医疗、采购、知识库问答等业务系统。

先记住一个总原则：

```text
Embabel 的主抽象不是“一个大 Prompt + 一个万能 Agent”，
而是：强类型领域对象 + Action + Goal + Condition + Planner。
```

也就是说，Embabel 更鼓励你把业务拆成可测试、可追踪、可组合的小步骤，再让框架根据目标和当前状态选择下一步。

| Agent 模式 | 框架入口 / 核心类 | 核心思想 | 适用场景 | 不适合场景 | 合同智审例子 | 其他场景例子 |
|---|---|---|---|---|---|---|
| Focused Agent | `AgentInvocation` / `AgentPlatform.runAgentFrom` | 代码明确指定运行哪个 Agent | 业务系统中已经知道要执行哪个智能能力 | 需要系统自己理解意图、选择 Agent 或选择目标 | 用户点击“开始审查”，系统直接运行合同审查 Agent | 用户点击“生成投研报告”“开始理赔审核”“执行运维巡检” |
| GOAP Agent | `PlannerType.GOAP` / `AStarGoapPlanner` | 根据 Action 输入输出、Condition 和 Goal 动态规划步骤 | 目标明确、步骤可拆、依赖关系清楚的企业业务流程 | 完全开放式探索任务，或者没有明确目标的任务 | 从合同正文自动规划出“抽取信息 → 识别风险 → 生成报告” | 投研报告生成、采购合规检查、保险理赔审核 |
| Utility Agent | `PlannerType.UTILITY` / `UtilityInvocation` | 每轮选择当前收益最高的 Action，不一定要求有最终 Goal | 探索型、开放式、价值驱动任务 | 强合规、强顺序、强审计的主流程 | 自动探索合同还有哪些方面值得补查 | 安全巡检、客户画像补全、线索挖掘 |
| Hybrid Agent | `PlannerType.HYBRID` | 像 Utility 一样选择高价值 Action，但达到目标后干净终止 | 既需要探索，又有明确终点 | 简单线性流程，或者每一步都必须严格固定 | 边补充风险分析，边在报告达标后停止 | 研究助理边搜集资料边在结论足够后停止 |
| Supervisor Agent | `PlannerType.SUPERVISOR` / `SupervisorInvocation` | LLM 像主管一样查看已有材料，决定下一步调用哪个 Action 或 Tool | 非标准任务、工具组合、路径不清楚的开放调度 | 高确定性、强审计、必须可复现的主流程 | 非标准合同中，让 LLM 决定先查政策库、拆条款还是补充材料 | 企业知识助理、运维故障排查、财务凭证核查 |
| Tool-Calling / ReAct 风格 Agent | `ToolLoop` / `@LlmTool` / MCP Tool | LLM 多轮调用工具，观察工具结果后继续决策 | 查询外部系统、检索、工具辅助生成 | 把所有业务流程都交给 LLM 自由发挥 | 检索标准条款库、法规库、历史合同库 | 查 CRM、查工单、查数据库、调用搜索工具 |
| Subagent / Nested Agent | `RunSubagent` / `ActionContext.asSubProcess` | 一个 Agent 或 Action 内部启动另一个 Agent / 子流程 | 复杂任务拆成多个可独立理解和测试的子任务 | 简单流程，过早拆分会增加复杂度 | 主合同审查 Agent 调“附件审查 Agent”或“发票审查 Agent” | 主运维 Agent 调“数据库诊断 Agent”“网络诊断 Agent” |
| Open Mode Agent | `Autonomy.chooseAndAccomplishGoal` | 系统从平台上的所有 Goal 中选择目标，并可组合可用能力 | 平台型智能助理、自然语言任务入口 | 不希望系统跨业务域组合能力的强边界场景 | 用户说“帮我处理这份合同”，系统自己选择合同审查目标 | 企业统一智能入口、政务事项办理助手 |
| Closed Mode Agent | `Autonomy.chooseAndRunAgent` | 系统先选择合适 Agent，再只在该 Agent 内运行 | 多 Agent 应用入口，但希望运行边界受控 | 单一按钮式业务，或需要跨 Agent 组合能力 | 用户输入“审合同 / 问制度 / 生成函件”，系统选择对应 Agent | 多技能客服机器人、多业务助手入口 |

### 3.1 初学者应该优先掌握哪些 Agent 模式

第一阶段只需要掌握三个：

```text
1. Focused Agent：我明确知道要运行哪个 Agent。
2. GOAP Agent：我知道最终目标，但不想手写所有步骤顺序。
3. Tool-Calling / ReAct 风格能力：某一步需要 LLM 调工具查资料。
```

第二阶段再学习：

```text
4. Supervisor Agent：路径不清楚时，让 LLM 调度工具和 Action。
5. Subagent：复杂系统拆成多个小 Agent。
6. Utility / Hybrid：探索型任务或价值驱动任务。
```

### 3.2 ReAct / ReactAgent 在 Embabel 里怎么理解

很多人从 LangChain、LangGraph、Dify 或 Coze 接触 Agent，会先听到 ReActAgent。

ReAct 的核心不是某个类名，而是一种循环：

```text
LLM 观察当前上下文
  ↓
LLM 决定调用哪个工具
  ↓
工具返回结果
  ↓
LLM 根据结果继续决策
  ↓
直到输出最终答案
```

Embabel 支持这种能力，主要体现在：

- `ToolLoop`
- `@LlmTool`
- MCP Tool
- Tool group
- parallel tool loop
- Supervisor Agent 内部的工具调度

但是 Embabel 不建议把所有企业业务都写成一个自由 ReActAgent。更推荐：

| 常见 ReAct 写法 | Embabel 推荐写法 |
|---|---|
| 一个 Agent + 很多 tools | 多个强类型 Action + 明确 Goal |
| Prompt 里描述完整流程 | 用输入输出类型、Condition 和 Goal 表达流程 |
| LLM 自己决定所有步骤 | Planner / Condition 决定确定性步骤，LLM 只处理语义任务 |
| 返回自然语言文本 | 返回结构化 Domain Model |
| 工具结果靠 Prompt 解释 | 工具和 Action 产出强类型对象，进入 blackboard |

一句话总结：

```text
Embabel 支持 ReAct 风格工具循环，
但 Embabel 的主线是类型化 Agentic Workflow，
不是把所有业务都塞进一个 ReactAgent。
```

---

## 4. Embabel 预置 Workflow 技术全局矩阵

Agent 模式解决“谁来决定下一步”。Workflow 技术解决“某些结构化流程怎么表达”。

Embabel 里的 workflow 不是传统 BPMN、Airflow、Temporal 那种固定流程引擎。它更偏 **agentic workflow**：

- 可以通过 GOAP 动态规划执行路径。
- 也可以显式表达循环、并行、汇总、仲裁等结构。
- 显式 workflow 可以作为一个 Agent，也可以嵌入某个 Action 里作为子流程。

| Workflow 技术 | 框架入口 / 核心类 | 核心思想 | 适用场景 | 不适合场景 | 合同智审例子 | 其他场景例子 |
|---|---|---|---|---|---|---|
| Simple Workflow / Simple Agent | `SimpleAgentBuilder` | 用一个简单 Action 封装一个输入到一个结果的能力 | 最小智能能力、教学示例、简单转换 | 多步骤复杂流程 | 输入合同文本，生成合同摘要 | 输入客户信息，生成客户画像摘要 |
| Dynamic Planning Workflow | `PlannerType.GOAP` / GOAP Planner | 不手写流程图，由 Planner 根据当前对象、条件和目标规划路径 | 目标明确但路径可能变化 | 完全固定的审批流程，或无明确目标任务 | 根据已有合同数据自动跳过已完成步骤 | 理赔审核根据已有材料自动规划补充步骤 |
| Repeat Until | `RepeatUntilBuilder` | 重复执行任务，直到条件满足或达到最大次数 | 结果需要迭代改进，有明确停止条件 | 一次即可确定完成的任务 | 反复生成报告，直到包含所有必需章节 | 反复生成营销文案直到满足品牌规范 |
| Repeat Until Acceptable | `RepeatUntilAcceptableBuilder` | 生成 → 评价 → 根据分数或反馈决定是否继续 | 有质量评分、反馈闭环的生成任务 | 没有评价标准的开放任务 | 报告质量评分低于 0.8 时继续重写 | 投研摘要评分不足时继续补充证据 |
| Scatter-Gather | `ScatterGatherBuilder` | 多路并行生成结果，再统一汇总 | 并行审查、并行检索、并行候选生成 | 强顺序依赖流程 | 规则审查、AI 审查、RAG 审查并行跑 | 多源舆情分析、多渠道数据核验 |
| Consensus | `ConsensusBuilder` | 多来源、多模型或多策略结果仲裁出最终结果 | 多模型投票、多专家意见合并、降低单模型偏差 | 单一确定性判断 | 多个模型分别判断高风险条款，最后合并 | 多评级模型综合评分、多专家诊断汇总 |
| SubProcess Workflow | `ActionContext.asSubProcess` / `RunSubagent` | 在某个 Action 内运行一个子 Agent 或子 workflow | 局部复杂流程复用、主流程保持简洁 | 简单 Action，或无需复用的小逻辑 | 报告生成 Action 内部运行“报告质量优化流程” | 运维诊断中调用“数据库专项诊断流程” |
| Parallel Tool Loop | `ParallelToolLoop` | 同一轮 LLM 返回多个工具调用时并行执行 | 多个 I/O 型工具互不依赖，想降低延迟 | 工具之间存在顺序依赖 | 同时检索法规库、模板库、历史案例库 | 同时查库存、订单、物流、客服工单 |

### 4.1 Workflow 和 Agent 的关系

不要把 Workflow 理解成 Agent 的替代品。更准确的关系是：

```text
Agent 负责表达能力边界和目标。
Planner 负责决定下一步。
Workflow 负责表达局部结构，例如循环、并行、仲裁、子流程。
```

例如合同智审可以这样组合：

```text
ContractReviewAgent
  ├─ extractMetadata                普通 Action
  ├─ splitClauses                   普通 Action
  ├─ detectRisks                    普通 Action
  ├─ runParallelRiskChecks           Scatter-Gather 子流程
  ├─ generateReport                  普通 Action
  └─ improveReportUntilAcceptable    RepeatUntilAcceptable 子流程
```

---

## 5. Agent 模式和 Workflow 技术怎么搭配

实际开发时通常不是“选一个模式就结束”，而是选择一个主 Agent 模式，再在局部步骤里使用 Workflow 技术。

| 需求类型 | 推荐组合 | 说明 |
|---|---|---|
| 标准企业业务流程 | GOAP Agent + Action / Goal / Condition | 最稳妥，适合大多数可建模业务 |
| 用户点击明确按钮 | Focused Agent + `AgentInvocation` | 业务代码明确知道要运行哪个 Agent |
| 自然语言统一入口 | Closed Mode 或 Open Mode + GOAP | 系统先理解用户意图，再选择 Agent 或 Goal |
| 开放式工具调度 | Supervisor Agent + ToolLoop / MCP Tool | LLM 负责任务调度，工具负责执行 |
| 质量迭代生成 | GOAP Agent + RepeatUntilAcceptable | 主流程确定，局部生成反复优化 |
| 多策略并行分析 | GOAP Agent + ScatterGather | 主流程中某一步需要并行生成多个结果 |
| 多模型仲裁 | Consensus + typed result | 多个模型或策略给出候选结果，再合并 |
| 复杂系统拆分 | Parent Agent + Subagent | 子任务独立建模、独立测试、独立演进 |
| 探索型任务 | Utility Agent 或 Hybrid Agent | 不知道下一步一定是什么，用 value/cost 决定 |

工程上建议按下面顺序选择，越靠前越确定、越容易测试：

```text
1. 能用普通代码规则，就不用 LLM。
2. 能用强类型 Action + GOAP 明确建模，就不用 Supervisor。
3. 只是局部需要循环、并行、仲裁，就用 Workflow Builder。
4. 只有任务路径不清楚、需要开放调度时，才让 LLM 当 Supervisor。
5. 只有任务本身是探索型、没有单一明确终点时，才优先考虑 Utility。
```

这条原则对高可靠业务尤其重要。合同、金融、医疗、基础设施等场景里，LLM 应该处理语义理解和生成，不应该无边界地决定所有业务步骤。

---

## 6. 如何选择 Agent 模式和 Workflow 技术

如果不知道该用哪种模式，可以先按下面的决策树判断。

```text
你是否已经知道要运行哪个业务能力？
├─ 是：使用 Focused Agent / AgentInvocation
│
└─ 否：是否希望系统从多个 Agent 中选择？
   ├─ 是，但只能在单个 Agent 内执行：Closed Mode
   └─ 是，而且可以跨 Agent / Goal 组合能力：Open Mode

你的任务是否有明确最终目标？
├─ 是：优先 GOAP Agent
│  ├─ 中间某一步需要反复优化：加入 RepeatUntil / RepeatUntilAcceptable
│  ├─ 中间某一步需要并行分析：加入 ScatterGather
│  └─ 中间某一步需要多模型仲裁：加入 Consensus
│
└─ 否：是否是探索型任务？
   ├─ 是：Utility / Hybrid
   └─ 否：先重新定义业务 Goal，不要急着上 Agent

是否需要 LLM 自主决定调用哪些工具？
├─ 是：Supervisor / ToolLoop
└─ 否：用 Action + Planner 明确建模
```

### 6.1 快速选型表

| 如果你想做的是…… | 优先选择 |
|---|---|
| 从输入材料生成一个明确结果 | GOAP Agent |
| 根据已有对象自动决定下一步 | GOAP Agent |
| 用户点击按钮后运行固定智能能力 | Focused Agent |
| 用户自然语言表达意图，系统选择 Agent | Closed Mode |
| 用户自然语言表达目标，系统组合全平台能力 | Open Mode |
| 让 LLM 自主调度多个工具 | Supervisor Agent |
| 让 LLM 多轮调用工具查资料 | ToolLoop / LLM Tool / MCP Tool |
| 结果不满意就继续生成 | RepeatUntil / RepeatUntilAcceptable |
| 多路并行分析后汇总 | ScatterGather |
| 多模型或多策略仲裁 | Consensus |
| 主流程里调用一个小 Agent | Subagent / SubProcess |
| 没有明确终点，只想探索最高价值动作 | Utility Agent |

---

## 7. 回到合同智审：推荐采用哪些模式

对于第一版合同智审，不建议一上来就做一个“全能 Supervisor”或“万能 ReactAgent”。更稳妥的方案是：

| 合同智审环节 | 推荐模式 / 技术 | 原因 |
|---|---|---|
| 用户点击开始审查 | Focused Agent | 业务入口明确，直接运行合同审查 Agent |
| 主审查流程 | GOAP Agent | 目标明确：生成 `ContractReviewReport`，中间步骤可由输入输出类型推导 |
| 合同基本信息抽取 | 普通 Action + AI structured output | 输入输出清楚，便于测试和追踪 |
| 确定性风险判断 | 普通代码 Action / Condition | 金额、日期、缺失字段等规则不应交给 LLM 判断 |
| 语义风险识别 | AI Action | 需要理解条款语义、模糊表达和上下文 |
| 制度 / 标准条款库检索 | RAG + ToolLoop / MCP Tool | 需要调用外部知识库或检索系统 |
| 规则审查、AI 审查、RAG 审查并行 | ScatterGather | 多路检查互不依赖，可以并行后汇总 |
| 多模型判断高风险条款 | Consensus | 降低单模型偏差，提升关键判断稳健性 |
| 审查报告质量优化 | RepeatUntilAcceptable | 有明确评分或验收标准时，适合生成-评价-重写闭环 |
| 审查附件、发票、补充协议 | Subagent | 每类材料可以有独立 Agent，主流程负责整合 |
| 非标准合同开放探索 | Supervisor Agent | 路径不清楚时，让 LLM 调度可用工具，但不作为第一版主流程 |

推荐的第一版组合：

```text
Focused Agent
  + GOAP Agent 主流程
  + 强类型 Domain Model
  + 普通代码规则 Action
  + AI 语义 Action
  + RAG 检索 Tool
```

进阶版再加入：

```text
ScatterGather
  + Consensus
  + RepeatUntilAcceptable
  + Subagent
  + Supervisor Agent
```

这样做的好处是：第一版足够稳定、可测试、可解释；后续又能自然扩展到多模型、多工具、多子流程的复杂系统。

---

## 8. 开发合同智审应用时，应该用哪些 Embabel 模块

### 8.1 最小 Web 应用推荐依赖

如果你要开发一个 Spring Boot Web 应用，推荐显式引入：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<dependency>
    <groupId>com.embabel.agent</groupId>
    <artifactId>embabel-agent-starter-webmvc</artifactId>
</dependency>
```

原因：

- `spring-boot-starter-web`：提供标准 Spring Boot Web 能力，例如 Controller、JSON、Tomcat、文件上传。
- `embabel-agent-starter-webmvc`：提供 Embabel Agent 的 WebMVC 集成能力。


容易混淆的一点：

```text
spring-boot-starter-web
  = 让你的应用具备“普通 Spring Boot Web 应用”的能力
  = 你可以写 @RestController、@PostMapping、MultipartFile 上传等代码

embabel-agent-starter-webmvc
  = 在 WebMVC 应用里额外加入 Embabel 已经写好的 Web 接口组件
  = 例如 Embabel 自带的 AgentProcess 状态查询、进程控制、SSE 事件接口等
```

所以，下面第 12 节里的 `ContractController` 伪代码，本质上是**你自己的业务 Web API**，主要使用的是 `spring-boot-starter-web` 提供的 Spring MVC 编程模型。

`embabel-agent-starter-webmvc` 的作用不是让你写 `@RestController`，而是把 Embabel 框架内部已有的 WebMVC 组件也注册进你的应用。例如本仓库中 `embabel-agent-webmvc` 模块包含类似这样的内置 Controller：

```kotlin
@RestController
@RequestMapping("/api/v1/process")
class AgentProcessStatusController(
    private val agentPlatform: AgentPlatform,
) {
    // 查询 Embabel AgentProcess 的状态和结果
}
```

也就是说：

```text
你的 ContractController
  = 你的业务接口
  = 通常由 spring-boot-starter-web 支撑

Embabel 自带的 AgentProcessStatusController / SseController
  = Embabel 框架接口
  = 由 embabel-agent-starter-webmvc 引入
```

在合同智审应用中，最常见的组合是：

```text
spring-boot-starter-web
  负责：合同创建、合同上传、开始审查、查询报告等你的业务 HTTP API

embabel-agent-starter-webmvc
  负责：Embabel Agent 执行过程的 WebMVC 集成，例如进程状态、SSE 状态流、框架级 REST 能力

embabel-agent-api
  负责：ContractReviewAgent、@Action、@Goal 等智能审查流程
```

因此，不要把“所有 Controller 代码”都理解成 `embabel-agent-starter-webmvc` 的专属代码。更准确的理解是：`embabel-agent-starter-webmvc` 让 Embabel 可以舒服地运行在 Spring WebMVC 应用里，并提供 Embabel 自己的 Web 端点；你的业务 Controller 仍然是普通 Spring Boot Web 开发。

---

### 8.2 常用模块和合同智审场景对应关系

| 业务场景 | 推荐模块 | 作用 |
|---|---|---|
| 定义 Agent、Action、Goal | `embabel-agent-api` | 核心编程模型 |
| 开发 REST API | `embabel-agent-starter-webmvc` | WebMVC 集成 |
| 命令行调试 Agent | `embabel-agent-shell` / `starter-shell` | 本地交互式调试 |
| 调用 OpenAI 模型 | `embabel-agent-starter-openai` | OpenAI 模型接入 |
| 调用 Ollama 本地模型 | `embabel-agent-starter-ollama` | 本地模型接入 |
| 本地 embedding | `embabel-agent-onnx` / `starter-onnx` | 本地向量模型 |
| 企业制度/标准条款库检索 | `embabel-agent-rag-*` | RAG 检索增强 |
| 文档/附件解析 | `embabel-agent-rag-tika` | 解析文本、HTML、Markdown 等 |
| 审查链路观测 | `embabel-agent-observability` | tracing、metrics、审计 |
| 对外暴露工具能力 | `embabel-agent-mcp` | MCP Server 集成 |
| 多 Agent 协作 | `embabel-agent-a2a` | Agent-to-Agent 协议 |

---

## 9. 合同智审的推荐业务对象

第一版不要设计得太复杂。建议先有这些对象。

### 9.1 合同文本

```java
public record ContractText(
    String contractId,
    String content
) {}
```

### 9.2 合同基本信息

```java
public record ContractMetadata(
    String contractTitle,
    String contractType,
    String partyA,
    String partyB,
    String amount,
    String currency,
    String effectiveDate,
    String expiryDate
) {}
```

### 9.3 合同条款

```java
public record ContractClause(
    String clauseNo,
    String title,
    String content,
    ClauseType type
) {}
```

```java
public enum ClauseType {
    PAYMENT,
    DELIVERY,
    ACCEPTANCE,
    LIABILITY,
    CONFIDENTIALITY,
    INTELLECTUAL_PROPERTY,
    TERMINATION,
    DISPUTE_RESOLUTION,
    OTHER
}
```

### 9.4 风险项

```java
public record ContractRiskFinding(
    String clauseNo,
    String clauseTitle,
    RiskLevel level,
    String description,
    String basis,
    String suggestion
) {}
```

```java
public enum RiskLevel {
    LOW,
    MEDIUM,
    HIGH
}
```

### 9.5 审查报告

```java
public record ContractReviewReport(
    String contractId,
    ContractMetadata metadata,
    List<ContractRiskFinding> risks,
    String summary,
    String conclusion
) {}
```

---

## 10. 第一个合同审查 Agent 应该怎么写

第一版建议只做 3 个 Action：

```text
extractMetadata  → 抽取基本信息
detectRisks      → 识别风险
generateReport   → 生成报告
```

伪代码：

```java
@Agent(description = "合同智能审查 Agent")
public class ContractReviewAgent {

    @Action
    public ContractMetadata extractMetadata(ContractText contractText, Ai ai) {
        String prompt = """
            你是合同审查助手。
            请从以下合同正文中抽取：
            1. 合同名称
            2. 合同类型
            3. 甲方
            4. 乙方
            5. 金额
            6. 币种
            7. 生效日期
            8. 终止日期

            合同正文：
            %s
            """.formatted(contractText.content());

        return ai.createObject(prompt, ContractMetadata.class);
    }

    @Action
    public List<ContractRiskFinding> detectRisks(ContractText contractText, ContractMetadata metadata, Ai ai) {
        String prompt = """
            你是企业法务合同审查助手。
            请审查以下合同是否存在风险。

            重点关注：
            1. 付款条件是否明确
            2. 验收标准是否缺失
            3. 违约责任是否过重或不对等
            4. 争议解决条款是否不利
            5. 知识产权归属是否清晰
            6. 保密条款是否过宽

            请输出风险等级、风险说明、依据和修改建议。

            合同基本信息：
            %s

            合同正文：
            %s
            """.formatted(metadata, contractText.content());

        return ai.createObject(prompt, List.class);
    }

    @AchievesGoal(description = "生成合同审查报告")
    @Action
    public ContractReviewReport generateReport(
        ContractText contractText,
        ContractMetadata metadata,
        List<ContractRiskFinding> risks,
        Ai ai
    ) {
        String prompt = """
            请根据合同基本信息和风险清单生成一份合同审查报告。
            报告需要包含：
            1. 合同摘要
            2. 风险总览
            3. 高风险问题
            4. 修改建议
            5. 是否建议签署

            合同ID：%s
            基本信息：%s
            风险清单：%s
            """.formatted(contractText.contractId(), metadata, risks);

        return ai.createObject(prompt, ContractReviewReport.class);
    }
}
```

上面的重点不是语法，而是思路：

```text
合同正文
  ↓
ContractMetadata
  ↓
List<ContractRiskFinding>
  ↓
ContractReviewReport
```

每一步都是结构化对象，而不是一大段散乱文本。

---

## 11. 为什么不要一开始只写一个 reviewContract 方法

新手容易这样写：

```java
public String reviewContract(String contractText) {
    return callLlm("请审查这个合同：" + contractText);
}
```

这能做 demo，但不适合真实系统。

问题：

1. 输出不可控。
2. 不方便测试。
3. 不方便定位是哪个步骤错了。
4. 不方便复用中间结果。
5. 不方便做人工复核。
6. 不方便接入企业标准条款库。
7. 不方便留痕和审计。

更好的拆法：

```text
extractMetadata
splitClauses
classifyClauses
checkMissingClauses
detectPaymentRisks
detectLiabilityRisks
matchStandardClauses
generateRevisionSuggestions
generateReport
```

这样每个步骤都可以单独调试、单独测试、单独替换。

---

## 12. Web API 应该怎么设计

第一版可以非常简单。

### 12.1 创建合同

```http
POST /api/contracts
Content-Type: application/json
```

请求：

```json
{
  "contractTitle": "采购合同",
  "businessOwner": "张三"
}
```

响应：

```json
{
  "contractId": "C-2026-0001"
}
```

伪代码：

```java
@RestController
@RequestMapping("/api/contracts")
public class ContractController {

    @PostMapping
    public CreateContractResponse createContract(@RequestBody CreateContractRequest request) {
        String contractId = contractService.create(request);
        return new CreateContractResponse(contractId);
    }
}
```

---

### 12.2 上传合同正文

```http
POST /api/contracts/{contractId}/text
Content-Type: application/json
```

请求：

```json
{
  "content": "甲方：……乙方：……合同金额：……"
}
```

伪代码：

```java
@PostMapping("/{contractId}/text")
public void uploadText(
    @PathVariable String contractId,
    @RequestBody UploadContractTextRequest request
) {
    contractService.saveText(contractId, request.content());
}
```

---

### 12.3 上传附件

```http
POST /api/contracts/{contractId}/attachments
Content-Type: multipart/form-data
```

伪代码：

```java
@PostMapping("/{contractId}/attachments")
public void uploadAttachment(
    @PathVariable String contractId,
    @RequestParam("file") MultipartFile file
) {
    contractFileService.save(contractId, file);
}
```

附件后续可以交给文档解析组件处理。

对应 Embabel/RAG 能力：

```text
embabel-agent-rag-tika → 解析附件文本
embabel-agent-rag-core → 表示文档、片段、来源
embabel-agent-rag-pipeline → 做检索增强
```

---

### 12.4 启动合同审查

```http
POST /api/contracts/{contractId}/review
```

响应：

```json
{
  "reviewRunId": "R-2026-0001",
  "status": "RUNNING"
}
```

伪代码：

```java
@PostMapping("/{contractId}/review")
public StartReviewResponse startReview(@PathVariable String contractId) {
    ContractText contractText = contractService.loadContractText(contractId);

    String runId = agentReviewService.startReview(contractText);

    return new StartReviewResponse(runId, "RUNNING");
}
```

Agent 调用伪代码：

```java
@Service
public class AgentReviewService {

    private final AgentPlatform agentPlatform;

    public String startReview(ContractText contractText) {
        return agentPlatform.runAsync(
            "请完成这份合同的智能审查并生成审查报告",
            contractText
        );
    }
}
```

注意：实际 API 名称需要以项目中的 `AgentPlatform` 真实接口为准。这里表达的是调用关系。

---

### 12.5 查询审查结果

```http
GET /api/contracts/{contractId}/review-report
```

响应：

```json
{
  "contractId": "C-2026-0001",
  "metadata": {
    "contractTitle": "采购合同",
    "contractType": "采购",
    "partyA": "某某公司",
    "partyB": "供应商公司",
    "amount": "1000000"
  },
  "risks": [
    {
      "clauseNo": "第5条",
      "clauseTitle": "付款条款",
      "level": "HIGH",
      "description": "付款条件未与验收结果绑定",
      "basis": "企业采购制度要求付款应以验收合格为前提",
      "suggestion": "建议修改为验收合格并收到合规发票后付款"
    }
  ],
  "summary": "本合同存在1项高风险问题。",
  "conclusion": "建议修改后再签署。"
}
```

伪代码：

```java
@GetMapping("/{contractId}/review-report")
public ContractReviewReport getReviewReport(@PathVariable String contractId) {
    return contractReviewResultService.getReport(contractId);
}
```

---

## 13. 加入 RAG 后，合同智审会怎样升级

第一版可以不做 RAG，只让 AI 根据合同文本审查。

但企业级合同智审一定需要 RAG，因为审查依据通常来自：

```text
企业合同模板
标准条款库
采购制度
财务付款制度
印章管理制度
数据安全制度
历史审查意见
历史谈判案例
```

RAG 的作用是：

```text
当前合同条款
  ↓
检索相关制度 / 标准条款 / 历史案例
  ↓
把检索结果交给 AI
  ↓
生成有依据的风险判断和修改建议
```

伪代码：

```java
@Action
public List<StandardClauseMatch> matchStandardClauses(
    List<ContractClause> clauses,
    RagService ragService
) {
    List<StandardClauseMatch> matches = new ArrayList<>();

    for (ContractClause clause : clauses) {
        RagResponse response = ragService.search(
            "查找与以下合同条款相关的企业标准条款：" + clause.content()
        );

        matches.add(new StandardClauseMatch(clause, response.results()));
    }

    return matches;
}
```

再让 AI 基于标准条款判断偏离：

```java
@Action
public List<ContractRiskFinding> detectDeviationRisks(
    List<ContractClause> clauses,
    List<StandardClauseMatch> standardMatches,
    Ai ai
) {
    return ai.createObject(
        "请比较当前合同条款和企业标准条款，识别偏离和风险",
        List.class
    );
}
```

---

## 14. 哪些事情适合用规则，哪些适合用 AI

合同智审不能所有事情都交给 AI。

### 14.1 适合用普通代码规则

这些判断确定性强，应该优先用代码：

```text
合同金额是否为空
甲乙方是否为空
金额大小写是否不一致
是否缺少争议解决条款
是否缺少付款条款
是否命中企业红线关键词
预付款比例是否超过制度阈值
合同期限是否超过授权范围
```

伪代码：

```java
@Action
public List<ContractRiskFinding> checkDeterministicRules(
    ContractMetadata metadata,
    List<ContractClause> clauses
) {
    List<ContractRiskFinding> risks = new ArrayList<>();

    if (metadata.amount() == null) {
        risks.add(new ContractRiskFinding(
            null,
            "合同金额",
            RiskLevel.HIGH,
            "合同金额缺失",
            "合同台账和审批要求必须明确合同金额",
            "请补充合同金额、币种和税费说明"
        ));
    }

    boolean hasDisputeClause = clauses.stream()
        .anyMatch(c -> c.type() == ClauseType.DISPUTE_RESOLUTION);

    if (!hasDisputeClause) {
        risks.add(new ContractRiskFinding(
            null,
            "争议解决",
            RiskLevel.MEDIUM,
            "缺少争议解决条款",
            "合同应明确管辖法院或仲裁机构",
            "建议补充争议解决条款"
        ));
    }

    return risks;
}
```

### 14.2 适合用 AI

这些判断需要理解语义，适合用 AI：

```text
违约责任是否明显不对等
付款条款是否对我方不利
验收条款是否模糊
保密义务是否过宽
知识产权归属是否存在歧义
对方修改是否引入新风险
条款措辞是否可能导致解释争议
```

伪代码：

```java
@Action
public List<ContractRiskFinding> checkSemanticRisks(
    List<ContractClause> clauses,
    Ai ai
) {
    return ai.createObject(
        "请逐条审查合同条款中是否存在语义层面的法律和商业风险",
        List.class
    );
}
```

正确架构应该是：

```text
规则检查结果 + AI 检查结果 + RAG 依据
  ↓
统一合并、去重、分级
  ↓
输出最终风险清单
```

---

## 15. 推荐的第一版开发范围

不要一开始做完整合同系统。第一版建议只做这个闭环：

```text
合同文本输入
  ↓
抽取基本信息
  ↓
识别合同类型
  ↓
识别风险点
  ↓
生成修改建议
  ↓
生成审查报告
  ↓
通过 REST API 返回 JSON
```

第一版暂时不要做：

```text
复杂权限
完整审批流
Word 修订痕迹
OCR
复杂文件存储
多租户
历史合同画像
复杂报表
```

这些都可以后续逐步加入。

---

## 16. 推荐学习顺序

### 第一步：理解项目结构

先看：

```text
README.md
pom.xml
embabel-agent-api/README.md
embabel-agent-starters/pom.xml
embabel-agent-starters/embabel-agent-starter-webmvc/pom.xml
```

你需要知道：

```text
api            = 核心编程模型
autoconfigure  = Spring Boot 自动配置
starters       = 使用者引入的组合依赖
webmvc         = Web 接口支持
rag            = 知识库检索增强
observability  = 观测和审计
```

---

### 第二步：学习最小 Java / Kotlin / Maven / Spring Boot

至少能看懂：

```text
Java class / record / enum / annotation
Kotlin data class / val / var
Maven pom.xml / dependency / module
Spring Boot @SpringBootApplication / @RestController / @Service
```

---

### 第三步：写一个最小 Agent

目标：

```text
输入一段合同文本
输出 ContractMetadata
```

先不要做风险审查。

---

### 第四步：加风险识别

目标：

```text
输入 ContractText + ContractMetadata
输出 List<ContractRiskFinding>
```

---

### 第五步：加 REST API

目标：

```text
POST /api/contracts/{id}/review
GET  /api/contracts/{id}/review-report
```

---

### 第六步：加 RAG

目标：

```text
合同条款 → 检索企业标准条款 → 生成有依据的风险建议
```

---

## 17. 合同智审应用的建议目录结构

如果你新建一个业务应用，可以类似这样组织：

```text
contract-review-app/
├── pom.xml
├── src/main/java/com/example/contractreview/
│   ├── ContractReviewApplication.java
│   ├── api/
│   │   └── ContractController.java
│   ├── agent/
│   │   └── ContractReviewAgent.java
│   ├── domain/
│   │   ├── ContractText.java
│   │   ├── ContractMetadata.java
│   │   ├── ContractClause.java
│   │   ├── ContractRiskFinding.java
│   │   └── ContractReviewReport.java
│   ├── service/
│   │   ├── ContractService.java
│   │   ├── ContractReviewService.java
│   │   └── ContractFileService.java
│   └── rag/
│       └── StandardClauseKnowledgeBase.java
└── src/main/resources/
    └── application.yml
```

职责划分：

| 目录 | 作用 |
|---|---|
| `api/` | REST API |
| `agent/` | Embabel Agent 定义 |
| `domain/` | 合同业务对象 |
| `service/` | 普通业务服务 |
| `rag/` | 标准条款库、制度库、检索相关 |
| `resources/` | Spring Boot 配置 |

---

## 18. 一个完整调用链示意

用户点击“开始审查”后，系统内部可以这样走：

```text
ContractController.startReview(contractId)
  ↓
ContractService.loadText(contractId)
  ↓
AgentReviewService.startReview(contractText)
  ↓
AgentPlatform 执行 ContractReviewAgent
  ↓
Action 1: extractMetadata
  ↓
Action 2: splitClauses
  ↓
Action 3: checkDeterministicRules
  ↓
Action 4: checkSemanticRisks
  ↓
Action 5: matchStandardClauses
  ↓
Action 6: generateReport
  ↓
ContractReviewReport
  ↓
保存审查结果
  ↓
GET /api/contracts/{id}/review-report 返回给前端
```

---

## 19. 最后给初学者的判断标准

1. `embabel-agent-api` 是干什么的？

   `embabel-agent-api` 是 Embabel Agent 的核心编程模型模块。它提供定义 Agent、Action、Goal、Condition、领域对象、Prompt、Tool、SPI 等核心能力。开发者写合同审查 Agent 时，主要依赖它来表达“有哪些动作”“最终目标是什么”“中间结果是什么”。

2. `starter-webmvc` 和 `spring-boot-starter-web` 有什么区别？

   `spring-boot-starter-web` 是 Spring Boot 官方 Web 应用 starter，提供 Web 容器、JSON、Spring MVC、Controller 等标准 Web 能力。`embabel-agent-starter-webmvc` 是 Embabel 的 WebMVC 集成 starter，用来把 Embabel Agent 的能力接入 WebMVC 场景。它不能简单替代 `spring-boot-starter-web`；开发 Web 应用时建议两个都显式引入。

3. 什么是 Agent？

   Agent 是一组智能业务能力的集合。在合同智审中，`ContractReviewAgent` 可以负责合同信息抽取、风险识别、标准条款匹配、修改建议生成和审查报告生成。它不是单个大模型调用，而是由多个明确 Action 组成的业务智能组件。

4. 什么是 Action？

   Action 是 Agent 可以执行的一个具体步骤。比如 `extractMetadata` 抽取合同基本信息，`detectRisks` 识别风险，`generateReport` 生成审查报告。Action 应该输入明确、输出明确，最好返回结构化业务对象，而不是随意的一段文本。

5. 什么是 Goal？

   Goal 是 Agent 要达成的业务目标，例如“生成合同审查报告”或“完成合同风险识别”。Embabel 会围绕 Goal 组织 Action 执行路径。开发时不要只想着“调用哪个方法”，而要表达“最终要得到什么结果”。

6. GOAP Agent、Utility Agent、Supervisor Agent 分别适合什么场景？

   GOAP Agent 适合目标明确、步骤可拆、输入输出依赖清楚的业务流程，是合同智审主流程的优先选择。Utility Agent 适合没有单一终点、需要按收益选择下一步的探索型任务。Supervisor Agent 适合路径不清楚、需要 LLM 像主管一样调度多个工具或 Action 的开放式任务，但不适合作为高确定性合同审查主流程的默认架构。

7. Workflow 技术里的 RepeatUntil、ScatterGather、Consensus 分别解决什么问题？

   RepeatUntil / RepeatUntilAcceptable 解决“生成结果后需要反复改进直到达标”的问题。ScatterGather 解决“多路并行分析后统一汇总”的问题。Consensus 解决“多个模型、多个专家或多种策略给出候选结果后需要仲裁”的问题。在合同智审里，它们分别可以用于报告质量优化、规则/AI/RAG 并行风险审查、多模型高风险条款判断。

8. 为什么合同审查结果要设计成结构化对象？

   因为结构化对象更可控、可测试、可展示、可存储、可审计。例如把风险项设计成 `ContractRiskFinding`，包含条款编号、风险等级、风险说明、依据和修改建议，前端可以直接展示，数据库可以保存，审批流可以根据风险等级路由，后续也能复盘和统计。

9. 哪些审查适合规则，哪些适合 AI？

   确定性强、边界清楚的审查适合规则，例如合同金额是否为空、是否缺少争议解决条款、预付款比例是否超过制度阈值、合同主体是否缺失。需要理解语义和上下文的审查适合 AI，例如违约责任是否不对等、付款条款是否对我方不利、知识产权归属是否存在歧义、保密义务是否过宽。真实系统应该规则和 AI 混合使用。

10. RAG 在合同智审里解决什么问题？

    RAG 用来把企业知识引入审查过程，例如合同模板、标准条款库、采购制度、财务付款制度、历史审查意见等。没有 RAG 时，AI 主要依赖合同文本和通用知识；有 RAG 后，系统可以先检索相关制度和标准条款，再基于这些依据判断当前合同是否偏离要求，并给出更可信的修改建议。

11. Web API 应该如何触发 Agent 执行？

    Web API 不应该直接在 Controller 里拼 prompt 调大模型。更好的方式是：Controller 接收请求，Service 加载合同正文和附件，构造 `ContractText` 等领域对象，然后调用 `AgentPlatform` 或封装后的 `AgentReviewService` 启动合同审查 Agent，最后保存并返回 `ContractReviewReport`。

12. 为什么不要把全部合同审查写成一个巨大的 prompt？

    因为巨大 prompt 难以测试、难以复用、难以定位错误、输出不稳定，也不利于人工复核和审计。合同审查应拆成多个步骤：基本信息抽取、条款切分、规则检查、AI 风险检查、标准条款匹配、报告生成。这样每一步都能独立验证，后续也更容易扩展。

如果你完全不懂这个项目，建议先不要看所有源码。先按本文做一个极小 demo：

```text
合同文本 → 基本信息抽取 → 风险识别 → JSON 报告
```

这个闭环跑通后，再逐步加入附件解析、标准条款库、审批流和观测审计。
